let price;// valid identifier

let price123; // valid identifier

let _price; // valid identifier

let $price; // valid identifier

// let old price; // not allowed due to space

// let 123price; // not allowed due to starting with a number

let priceNew=5; // valid identifier
console.log("Price using let: " + priceNew);
//  console.log("Not valid if we change the case sensitivity of the identifier" +PriceNew);

// let const; // not allowed as 'const' is a reserved keyword
// but we can use it as of 
let constantValue; // valid identifier

console.log("---------------------------------------------------");

// camelCase notation
let myVariableName = "Camel Case"; // valid identifier
// snake_case notation
let my_variable_name = "Snake Case"; // valid identifier
//PascalCase notation
let MyVariableName = "Pascal Case"; // valid identifier